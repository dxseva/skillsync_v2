[project]
name = "skillsync-pro"
version = "2.0.0"
description = "AI-powered career guide for students and graduates"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
